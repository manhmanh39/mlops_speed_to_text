data:
  extracted_dir: data/train
  meta_csv: data/train/metadata.csv

train:
  output_dir: models/wav2vec2-finetuned
  num_train_epochs: 20
  learning_rate: 1.0e-5
  per_device_train_batch_size: 8
  gradient_accumulation_steps: 6

eval:
  wav_dir: data/eval
  out_save_dir: outputs
